# This workflow will build a Java project with Ant
# For more information see: https://help.github.com/actions/language-and-framework-guides/building-and-testing-java-with-ant

name: Java CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Set up JDK 11
      uses: actions/setup-java@v2
      with:
        java-version: '11'
        distribution: 'adopt'
    - name: Build with Maven
      run: mvn -B package --file pom.xml
    - run: mkdir deployment && cp target/*.war deployment
    - run: pwd && ls && target/* && ls deployment/*
    - run: curl --upload-file deployment/simplewebapp.war "http://deployer:deployer@http://34.207.192.227:8080//manager/text/deploy?path=/githubaction&update=true
